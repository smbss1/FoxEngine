
SOME_CONSTANT :: 45;

test as struct
{
    var1 : float[15];
    var2 : *char;
    ca : int;
    ee : as struct {
        num : int;
        letter : char;
    };
    MyProc::func(fa: float) -> int;
    MyProc::func(fa: float, gduvfd: int) -> int;
    MyProc::func(fa: float, uvyft: *char) -> int;
};

fsf as union
{
    f : float;
    g : float;
};

essvg as enum
{
    FLAG_1,
    FLAG_2,
};